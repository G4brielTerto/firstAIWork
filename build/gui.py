
# This file was generated by the Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer

# CONSERTAR O TEXTO DE SAÍDA DO BOT QUE NÃO ESTÁ PULANDO PARA PRÓXIMA LINHA



from langchain_ollama import OllamaLLM
from langchain_core.prompts import ChatPromptTemplate

context = []

template = """
Answer the question below.

Here is the conversation history: {context}

Question: {question}

Answer:
"""

model = OllamaLLM(model="llama3")
prompt = ChatPromptTemplate.from_template(template)
chain = prompt | model



from pathlib import Path

# from tkinter import *
# Explicit imports to satisfy Flake8
# import tkinter as tk
# from tkinter import Tk, Canvas, Entry, Text, Button, PhotoImage, Label
from tkinter import *
import tkinter as tk


OUTPUT_PATH = Path(__file__).parent
ASSETS_PATH = OUTPUT_PATH / Path(r"C:\Users\Adrie\Desktop\acho que essa é a tela em definitivo\build\assets\frame0")


def relative_to_assets(path: str) -> Path:
    return ASSETS_PATH / Path(path)


window = Tk()

window.geometry("1184x890")
window.configure(bg = "#F0FFFC")



def pegar_texto():
    one_context = ""
    user_input = entry_1.get("1.0", tk.END)


    result = chain.invoke({"context": context, "question": user_input})
    # label_bot["text"] = result
    # print(texto_bot)
    label_bot.config(text=result)

    one_context += f"\nUser: {user_input}\n AI: {result}"
    context.append(one_context)


canvas = Canvas(
    window,
    bg = "#F0FFFC",
    height = 890,
    width = 1184,
    bd = 0,
    highlightthickness = 0,
    relief = "ridge"
)

canvas.place(x = 0, y = 0)
canvas.create_text(
    38.0,
    52.0,
    anchor="nw",
    text="Digite o prompt abaixo:",
    fill="#000000",
    font=("Inter", 32 * -1)
)

canvas.create_text(
    38.0,
    426.0,
    anchor="nw",
    text= "",
    fill="#000000",
    font=("Inter", 32 * -1)
)

button_image_1 = PhotoImage(
    file=relative_to_assets("button_1.png"))
button_1 = Button(
    image=button_image_1,
    borderwidth=0,
    highlightthickness=0,
    # command=lambda: print("button_1 clicked"),
    command= pegar_texto,
    relief="flat"
)
button_1.place(
    x=38.0,
    y=316.0,
    width=238.0,
    height=79.0
)

button_image_2 = PhotoImage(
    file=relative_to_assets("button_2.png"))
button_2 = Button(
    image=button_image_2,
    borderwidth=0,
    highlightthickness=0,
    command=window.quit,
    relief="flat"
)
button_2.place(
    x=354.0,
    y=316.0,
    width=238.0,
    height=79.0
)

canvas.create_rectangle(
    38.0,
    133.0,
    1115.0,
    275.0,
    fill="#D9D9D9",
    outline="")

entry_image_1 = PhotoImage(
    file=relative_to_assets("entry_1.png"))
entry_bg_1 = canvas.create_image(
    575.0,
    204.0,
    image=entry_image_1
)
entry_1 = Text(
    bd=0,
    bg="#D9D9D9",
    fg="#000716",
    highlightthickness=0
)
entry_1.place(
    x=53.0,
    y=152.0,
    width=1044.0,
    height=102.0
)
label_bot = Label(
    text="texto do bot",
    bg="#ffffff",
    fg="#000000",
    font=("Inter", 28 * -1),
    wraplength=1100,
    justify="left"
)
# label_bot.place(x=38.0, y=470.0, width=800, height=100)
label_bot.place(x=38.0, y=470.0, width=1100)



window.resizable(False, False)
window.mainloop()


